# Niro v0.1.73

## Summary

Fix mode authenticates reliably when Niro runs with Codex, and on macOS.

## Changes

- `niro fix` mints GitHub App tokens correctly when run with Codex.
- On macOS, environment values containing spaces or line breaks — such as a PEM private key — are read correctly.

## Security

None.

## Compatibility and upgrade

No action required.

## Known issues

- `checksums.txt` does not cover `niro.mcpb`.
- Release artifacts have no detached signatures, provenance attestation, or SBOM.
